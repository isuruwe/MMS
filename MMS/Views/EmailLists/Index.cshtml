@model IEnumerable<MMS.EmailList>

@{
    ViewBag.Title = "Index";
}

<section class="navbar navbar-fixed-top custom-navbar" role="navigation">
    <div class="container">

        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-left">
                <li>
                    <a href="@Url.Action("Index","Home")">HOME</a>

                </li>
                <li>
                    @*<div ng-show="showerror">
                        <span style="background-color:white">Error!</span>
                    </div>*@

                </li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="/MMS/EmailLists/Index">Back to Email List</a></li>
             
                @*<li><a href="#work">Lab Report</a></li>*@
                @*<li><a href="#about" class="smoothScroll">ABOUT</a></li>*@

                @*<li><a href="#portfolio" class="smoothScroll">PORTFOLIO</a></li>*@

            </ul>

        </div>



    </div>
</section>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>Service No</th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SvcID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SvcID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SvcID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SvcID })
        </td>
    </tr>
}

</table>
